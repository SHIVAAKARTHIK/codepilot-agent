# Core agent framework
deepagents
langgraph
langchain
langchain-community

# LLM providers (LLM_PROVIDER in .env selects which one is actually used;
# both are installed so switching is just an env var change)
langchain-openai
langchain-anthropic

# GitHub integration (GitHubToolkit depends on PyGithub)
PyGithub

# Vector store (semantic memory + embedding retrieval)
chromadb

# TUI
textual

# Config / env
python-dotenv

# Testing
pytest

# Dev quality-of-life
rich
